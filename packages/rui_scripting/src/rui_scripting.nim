## rui_scripting - public API barrel
##
## File-based GUI automation: query and set widget values for testing.
## (client.nim is a standalone driver tool, not part of the library API.)

import messages;       export messages
import selectors;      export selectors
import text_format;    export text_format
import script_manager; export script_manager
